#include <iostream>
using namespace std;

void check(int age) {
    if (age < 18)
        throw "Not eligible";
    else
        cout << "Eligible to vote";
}

int main() {
    try {
        check(16);
    }
    catch (const char* msg) {
        cout << msg;
    }

    return 0;
}
